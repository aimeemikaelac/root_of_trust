// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.1
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xcrypto_sign.h"

extern XCrypto_sign_Config XCrypto_sign_ConfigTable[];

XCrypto_sign_Config *XCrypto_sign_LookupConfig(u16 DeviceId) {
	XCrypto_sign_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XCRYPTO_SIGN_NUM_INSTANCES; Index++) {
		if (XCrypto_sign_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XCrypto_sign_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XCrypto_sign_Initialize(XCrypto_sign *InstancePtr, u16 DeviceId) {
	XCrypto_sign_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XCrypto_sign_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XCrypto_sign_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

