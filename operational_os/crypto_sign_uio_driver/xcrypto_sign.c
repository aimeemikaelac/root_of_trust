// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.1
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

/***************************** Include Files *********************************/
#include "xcrypto_sign.h"

/************************** Function Implementation *************************/
#ifndef __linux__
int XCrypto_sign_CfgInitialize(XCrypto_sign *InstancePtr, XCrypto_sign_Config *ConfigPtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(ConfigPtr != NULL);

    InstancePtr->Axilites_BaseAddress = ConfigPtr->Axilites_BaseAddress;
    InstancePtr->IsReady = XIL_COMPONENT_IS_READY;

    return XST_SUCCESS;
}
#endif

void XCrypto_sign_Start(XCrypto_sign *InstancePtr) {
    u32 Data;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XCrypto_sign_ReadReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_AP_CTRL) & 0x80;
    XCrypto_sign_WriteReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_AP_CTRL, Data | 0x01);
}

u32 XCrypto_sign_IsDone(XCrypto_sign *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XCrypto_sign_ReadReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_AP_CTRL);
    return (Data >> 1) & 0x1;
}

u32 XCrypto_sign_IsIdle(XCrypto_sign *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XCrypto_sign_ReadReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_AP_CTRL);
    return (Data >> 2) & 0x1;
}

u32 XCrypto_sign_IsReady(XCrypto_sign *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XCrypto_sign_ReadReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_AP_CTRL);
    // check ap_start to see if the pcore is ready for next input
    return !(Data & 0x1);
}

void XCrypto_sign_EnableAutoRestart(XCrypto_sign *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XCrypto_sign_WriteReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_AP_CTRL, 0x80);
}

void XCrypto_sign_DisableAutoRestart(XCrypto_sign *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XCrypto_sign_WriteReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_AP_CTRL, 0);
}

u32 XCrypto_sign_Get_return(XCrypto_sign *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XCrypto_sign_ReadReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_AP_RETURN);
    return Data;
}
void XCrypto_sign_Set_num_blocks(XCrypto_sign *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XCrypto_sign_WriteReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_NUM_BLOCKS_DATA, Data);
}

u32 XCrypto_sign_Get_num_blocks(XCrypto_sign *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XCrypto_sign_ReadReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_NUM_BLOCKS_DATA);
    return Data;
}

u32 XCrypto_sign_Get_sk_BaseAddress(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SK_BASE);
}

u32 XCrypto_sign_Get_sk_HighAddress(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SK_HIGH);
}

u32 XCrypto_sign_Get_sk_TotalBytes(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XCRYPTO_SIGN_AXILITES_ADDR_SK_HIGH - XCRYPTO_SIGN_AXILITES_ADDR_SK_BASE + 1);
}

u32 XCrypto_sign_Get_sk_BitWidth(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XCRYPTO_SIGN_AXILITES_WIDTH_SK;
}

u32 XCrypto_sign_Get_sk_Depth(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XCRYPTO_SIGN_AXILITES_DEPTH_SK;
}

u32 XCrypto_sign_Write_sk_Words(XCrypto_sign *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XCRYPTO_SIGN_AXILITES_ADDR_SK_HIGH - XCRYPTO_SIGN_AXILITES_ADDR_SK_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SK_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XCrypto_sign_Read_sk_Words(XCrypto_sign *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XCRYPTO_SIGN_AXILITES_ADDR_SK_HIGH - XCRYPTO_SIGN_AXILITES_ADDR_SK_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SK_BASE + (offset + i)*4);
    }
    return length;
}

u32 XCrypto_sign_Write_sk_Bytes(XCrypto_sign *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XCRYPTO_SIGN_AXILITES_ADDR_SK_HIGH - XCRYPTO_SIGN_AXILITES_ADDR_SK_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SK_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XCrypto_sign_Read_sk_Bytes(XCrypto_sign *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XCRYPTO_SIGN_AXILITES_ADDR_SK_HIGH - XCRYPTO_SIGN_AXILITES_ADDR_SK_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SK_BASE + offset + i);
    }
    return length;
}

u32 XCrypto_sign_Get_signature_out_BaseAddress(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_BASE);
}

u32 XCrypto_sign_Get_signature_out_HighAddress(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_HIGH);
}

u32 XCrypto_sign_Get_signature_out_TotalBytes(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_HIGH - XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_BASE + 1);
}

u32 XCrypto_sign_Get_signature_out_BitWidth(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XCRYPTO_SIGN_AXILITES_WIDTH_SIGNATURE_OUT;
}

u32 XCrypto_sign_Get_signature_out_Depth(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XCRYPTO_SIGN_AXILITES_DEPTH_SIGNATURE_OUT;
}

u32 XCrypto_sign_Write_signature_out_Words(XCrypto_sign *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_HIGH - XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XCrypto_sign_Read_signature_out_Words(XCrypto_sign *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_HIGH - XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_BASE + (offset + i)*4);
    }
    return length;
}

u32 XCrypto_sign_Write_signature_out_Bytes(XCrypto_sign *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_HIGH - XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XCrypto_sign_Read_signature_out_Bytes(XCrypto_sign *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_HIGH - XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Axilites_BaseAddress + XCRYPTO_SIGN_AXILITES_ADDR_SIGNATURE_OUT_BASE + offset + i);
    }
    return length;
}

void XCrypto_sign_InterruptGlobalEnable(XCrypto_sign *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XCrypto_sign_WriteReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_GIE, 1);
}

void XCrypto_sign_InterruptGlobalDisable(XCrypto_sign *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XCrypto_sign_WriteReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_GIE, 0);
}

void XCrypto_sign_InterruptEnable(XCrypto_sign *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XCrypto_sign_ReadReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_IER);
    XCrypto_sign_WriteReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_IER, Register | Mask);
}

void XCrypto_sign_InterruptDisable(XCrypto_sign *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XCrypto_sign_ReadReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_IER);
    XCrypto_sign_WriteReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_IER, Register & (~Mask));
}

void XCrypto_sign_InterruptClear(XCrypto_sign *InstancePtr, u32 Mask) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XCrypto_sign_WriteReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_ISR, Mask);
}

u32 XCrypto_sign_InterruptGetEnabled(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XCrypto_sign_ReadReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_IER);
}

u32 XCrypto_sign_InterruptGetStatus(XCrypto_sign *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XCrypto_sign_ReadReg(InstancePtr->Axilites_BaseAddress, XCRYPTO_SIGN_AXILITES_ADDR_ISR);
}

